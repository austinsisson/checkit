<div class="row"> 
	<div class="col-md-12" id="user-header">
		<h1>Welcome!</h1>
		<p>Here's everything you need to get done:</p>
		<br/><br/>
	</div>
</div>

<div class="row">
	<table class="table table-hover">
		<tr>
			<th>To-Do</th>
			<th>Time Left</th>
			<th>Finish</th>
			<th>Remove</th>
		</tr>
		<% @user.todos.each do |todo| %>
			<tr>
				<td id="todo-description"><%= todo.description %></td>
				<td id="todo-time_left"><%= todo.time_left %></td>
				<td id="todo-complete"><%= link_to 'Complete this task', user_todo_path(todo.user, todo, :todo => { :completed => true}), method: :put, class: 'btn btn-info', remote: true %></td>
				<td><%= link_to "Delete", user_todo_path(todo.user, todo), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this to-do?' } %></td>
			</tr>
		<% end %>
	</table>
<br/>
<div class="row">
	<div class="col-md-3 center-block">
		<p>What else do you need to do?</p>
		
		<%= form_for [@user, @user.todos.build] do |f| %>
			<%= f.text_field :description, placeholder: 'Description' %>
			<%= f.submit 'Save', class: 'btn btn-primary' %>
		<% end %>
	</div>
</div>
<br/><br/><br/>
<div class="row">
	<div class="col-md-12">
		<p class="text-center">Warning! All to-dos are automatically deleted after 7 days. Better get to work!</p>
	</div>
</div>
