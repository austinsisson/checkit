
<h1>Welcome!</h1>
<p>Below is what you need to do.</p>
<br/>

<% @user.todos.each do |todo| %>
	<p><%= todo.description %></p>
	<p><%= check_box_tag 'completed', todo.id, todo.completed %></p>
	<p><%= link_to "Delete to-do", user_todo_path(todo.user, todo), method: :delete %></p>
<% end %>

<br/><br/>
<p>What else do you need to do?<p>



<%= form_for [@user, @user.todos.build] do |f| %>
	<%= f.text_field :description, placeholder: 'Description' %>
	<%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
